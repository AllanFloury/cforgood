<div id="wrapper">
  <div class="card relative">
    <%= image_tag (current_user.picture.exists? ? current_user.picture : "profile.png"), class: "img-circle", style: "margin:auto;"  %>

    <%= image_tag "macarte.png", class: "img-card", style: "margin:auto;"%>
    <h2><%= current_user.first_name%> <%= current_user.last_name%> <%= current_user.name%> </h2>
  </div>
</div>