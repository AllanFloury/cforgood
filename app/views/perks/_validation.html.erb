<div class="container">
  <div class="row text-center margin-top40">
    <div class="col-xs-6 col-xs-offset-3">
      <%= simple_form_for(@perk, url: perk_perk_requests_path(@perk, @perk_request), method: :post) do |f| %>
        <%= f.button :submit, value: "J'en profite !", class: "btn btn-danger" %>
      <% end %>
    </div>
  </div>
</div>